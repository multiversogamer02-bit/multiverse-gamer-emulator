services:
  - type: web
    name: multiverse-server
    env: python
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: |
      alembic upgrade head &&
      uvicorn server.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: postgresql://multiverse_db_user:WdjaX02gEgg2n1eKEvI9rm8duFh0BjEq@dpg-d3ej318gjchc738pnd3g-a.oregon-postgres.render.com/multiverse_db
      - key: SECRET_KEY
        sync: false
      - key: MERCADOPAGO_ACCESS_TOKEN
        sync: false
      - key: MERCADOPAGO_WEBHOOK_SECRET
        sync: false  # ← Debe configurarse manualmente en Render
      - key: SENDGRID_API_KEY
        sync: false
      - key: SENDGRID_FROM_EMAIL
        sync: false
    healthCheckPath: /healthz